class Tuberculoso extends Legume
{
	string testeQualquer(string testeParam)
	{
	    int a = 9;
		boolean b = false;
	    if (b>a)
	    {
	    	/* o if acima retornara um erro, pois nao eh possivel comparar
	    	boolean com um inteiro utilizando um operador logico. */
	    }
	}
}
